# NHS Number API

> AI generated code for NHS RPySoc2025 demo - **do not use**

A FastAPI web wrapper around the `nhs_number` Python package.

## Installation

```bash
uv venv
source .venv/bin/activate
uv pip install -e .
```

## Usage

```bash
uv run nhs-number-api
```

Visit http://localhost to see possible routes.